# Genshin Pull Strategy Planner
#
# Python 3.12. Install with:
#
#     pip install -r requirements.txt
#
# Run the API (and the Phase 7 dashboard it serves) with:
#
#     uvicorn api.main:app --reload
#
# Run the tests with:
#
#     python -m pytest

# --- Engines (Phases 2, 4, 5) ----------------------------------------------
numpy>=1.26

# --- API (Phase 6) ---------------------------------------------------------
fastapi>=0.110
pydantic>=2.6
uvicorn[standard]>=0.27

# --- Development -----------------------------------------------------------
pytest>=8.0
httpx>=0.27          # required by fastapi.testclient
